pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
pip install anomalib albumentations matplotlib
conda install -c conda-forge faiss-cpu    # ← 任意。k-NN が高速化。conda使えないのでやらない